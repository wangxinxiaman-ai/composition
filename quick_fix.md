# 🚨 SERVICE_TIMEOUT 问题诊断

## 问题分析

您遇到的 `SERVICE_TIMEOUT` 错误可能由以下原因导致：

### 1. 冷启动超时（最可能 ⭐⭐⭐⭐⭐）
- **现象**: 容器首次启动时，需要：
  - 拉取镜像（10-30秒）
  - 启动Node.js进程（5-10秒）
  - 初始化CloudBase SDK（5-15秒）
  - 执行数据库集合检查（0-30秒）
- **结果**: 总计可能需要 **30-85秒**，超过CloudRun默认的60秒超时

### 2. 数据库初始化卡住
- **代码位置**: `server.js` 第47-65行
- **问题**: `db.createCollection('correction_tasks')` 可能因为权限或网络问题卡住

### 3. CloudRun配置问题
- **当前配置**: `MinNum=0`（允许缩容到0）
- **影响**: 每次无流量后，容器会被回收，下次请求触发冷启动

---

## 🎯 立即解决方案

### 方案1: 再试一次（推荐⭐⭐⭐⭐⭐）

**原因**: 冷启动只发生在第一次请求，第二次就会快很多。

**操作**:
1. 再次访问：
   ```
   https://composition-backend-199064-5-1376977951.sh.run.tcloudbase.com/api/test-db
   ```
2. 如果成功返回JSON → 🎉 问题解决！
3. 如果依然超时 → 尝试方案2

---

### 方案2: 修复数据库初始化代码

**问题**: `db.createCollection` 可能不支持或卡住

**修复**: 注释掉自动创建集合的代码（集合已经存在了）

<parameter name="content">