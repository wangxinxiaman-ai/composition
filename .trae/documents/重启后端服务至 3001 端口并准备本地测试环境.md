# 重启服务器与本地测试指引

**收到。** 为了确保所有修复（尤其是 `.env.local` 恢复和 `home.html` 修复）生效，必须重启服务。

**当前状态**：
*   后端服务正在 Terminal 4 运行（端口 80，因为上次启动没指定 PORT）。
*   `.env.local` 已恢复。
*   `server.js` 和 `home.html` 已修复。

**行动计划**：
1.  **停止旧服务**：终止 Terminal 4 的 `node server.js` 进程。
2.  **启动新服务**：使用端口 **3001** 启动（`PORT=3001 node server.js`），避免权限问题且符合本地开发习惯。
3.  **提供访问链接**：给出 `http://localhost:3001` 供您测试。

我立即执行。