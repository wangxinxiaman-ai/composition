# 修复变量重复声明错误

**报错原因**：
`SyntaxError: Identifier 'server' has already been declared`
这说明我在 `server.js` 中重复声明了 `server` 变量。
很抱歉，这是我刚才修改代码时引入的错误。可能在文件的其他地方（比如我没有检查到的地方）已经有了 `const server = ...`，或者我在刚才的修改中不小心保留了旧的声明。

**修复方案**：
1.  **检查 `server.js`**：找到所有 `server` 变量的声明。
2.  **清理代码**：确保 `server` 只被声明一次，并且正确引用 `app.listen` 的返回值。

我将立即修复这个语法错误。请在修复后重新部署。