# 前端增加请求超时控制

您提供的代码非常专业，它是标准的通过前端控制 `fetch` 请求超时的方法。
这虽然不能解决后端 60s 断开的问题（那个必须后端和网关配合），但它能让**前端**具备自我保护能力：如果后端真的挂了，前端可以优雅地提示“超时”，而不是无限等待。

鉴于您希望在前端加入这个逻辑，我将把这个 `fetchWithTimeout` 机制集成到您的 `home.html` 中，并将超时时间设置为 **5分钟**（300000ms），与我们后端设置的时间保持一致。

## 执行步骤

1.  **修改 `templates/home.html`**：
    *   定义 `fetchWithTimeout` 函数。
    *   找到原本调用 `fetch(API_BASE_URL + '/api/correct', ...)` 的地方。
    *   替换为 `fetchWithTimeout(API_BASE_URL + '/api/correct', ..., 300000)`。
    *   在 `catch` 块中处理 `AbortError`，给用户一个友好的提示（比如“AI 思考太久了，请重试”）。

2.  **上传更新**：修改完成后，将 `home.html` 重新上传到静态网站托管。

我将立即执行修改。