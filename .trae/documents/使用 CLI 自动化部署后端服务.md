# 紧急修复：server.js 括号闭合问题

**报错分析**：
`SyntaxError: Unexpected token 'const'` 在第 262 行 `const { spawn } = require('child_process');`。
这通常意味着**上一行代码没有正确结束**。
查看代码发现，我在修复 `buildStrictPrompt` 函数时，可能**多删了一个右括号 `}` 或者分号 `;`**，或者字符串拼接没有正确闭合，导致解析器认为函数还没结束，结果在函数内部遇到了 `const`（这是不合法的，或者上下文不对）。

**排查**：
我在第 259 行写了 `  );`，然后在第 260 行写了 `}`。
可能是我在拼接大字符串时，漏掉了字符串的结束引号，或者括号层级不对。

**修复方案**：
1.  **彻底检查 `buildStrictPrompt`**：确保所有字符串拼接都正确，所有括号都闭合。
2.  **验证**：修复后，我会在本地用 `node -c server.js`（语法检查）先跑一遍，确保没报错再部署，避免浪费您的时间。

我将立即修复。