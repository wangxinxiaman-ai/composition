# 本地测试引导：打开首页

**好的，先在本地跑起来看看效果！**
这最稳妥。本地测通了，心里就有底了。

**现状**：
1.  **后端服务**：已经在后台运行中（端口 3001）。
    *   *注：刚才我启动了它，它现在应该还在跑。如果不确定，我会再检查并启动一次。*
2.  **前端页面**：`templates/home.html`。

**如何在本地打开首页？**
因为这是一个前后端分离的项目（虽然 `server.js` 也托管了静态文件），您不能直接双击 `home.html` 打开（那样会有跨域问题且无法调用 API）。
**您必须通过浏览器访问 `http://localhost:3001`。**

**行动计划**：
1.  **确认后端在运行**：我用工具检查一下端口 3001 是否活着。如果没活，我重新启动 `node server.js`。
2.  **提供访问链接**：确认无误后，我把 `http://localhost:3001` 给您，您点开就能测。

**测试注意**：
本地测试时，上传图片后，后端会调用 Python 脚本和百度 OCR。
*   **Python 环境**：您本地必须装了 Python 且有 `preprocess.py` 需要的库（opencv-python 等）。如果没装，会报错（但页面会弹窗显示错误，正好测试报错机制）。
*   **API Key**：本地使用的是 `.env.local` 里的 Key。如果 Key 没配或额度没了，也会报错。

我先检查服务状态。